#include <stdio.h>
/**
 * main - A program that prints line of numbers
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i;
for (i = 0 ; i <= 10 ; i++)
{
printf(i);
}
putchar('\n');
return (0);
}
