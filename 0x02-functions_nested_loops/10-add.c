#include "main.h"
#include <stdio.h>
/**
 * add - a function that adds two integers and returns the result.
 *
 * @a : intiger character in ASCII
 *
 * @b : integer charachter in ASCII
 *
 * Return: 1 if lower , 0 if not  (Success)
 */
int add(int a, int b)
{
	return (a + b);
}
