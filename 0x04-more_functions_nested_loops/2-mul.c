#include "main.h"
#include<stdio.h>
/**
 * mul - a function that multiplies two integers.
 * @a: first number as input.
 * @b: second number as input.
 * Return: nultiplication of both inputs
 */
int mul(int a, int b)
{
	return (a * b);
}
